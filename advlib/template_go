package main

import (
        "fmt"

        "github.com/raff/advent2022/advlib"
)

func main() {
	r := advlib.NewReader()
        lines, _ := r.Readlines()

	for _, line := range lines {
		fmt.Println(advlib.Split(line))
	}
}
