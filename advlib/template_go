package main

import (
        "fmt"

        . "github.com/raff/advent2022/advlib"
)

func main() {
	r := NewReader()
        lines, _ := r.Readlines()

        if Part2 {
                fmt.Println("part 2")
        }

	for _, line := range lines {
		fmt.Println(Split(line))
	}
}
